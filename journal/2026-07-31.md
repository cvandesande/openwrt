# Session journal — 2026-07-31

Workspace restructure ahead of a planned machine wipe and reinstall. No
hardware or firmware work this session; nothing was flashed and the router was
not touched.

## Context

The meta workspace `mono-openwrt-project/` has **no git remote** and 7
local-only commits, so everything tracked in it — `AGENTS.md`, `README.md`,
`NOTES.md`, `docs/`, `scripts/`, `tools/` — plus the git-ignored `HANDOFF.md`
would be destroyed by the reinstall. That reframed a design discussion about
handoff documentation into a preservation task.

## Pre-wipe branch audit (openwrt)

Audited every local branch by **patch-id, not hash**, after a fresh fetch. The
fetch itself force-updated `main`, `mono-ask`, and `mono-oss`, so any
pre-fetch reading was unreliable.

Result: the ahead/behind counts were almost entirely bot-rewrite noise.

| Branch | Reported | Genuinely absent (patch-id) |
|---|---|---|
| `cmmqos-25.12` | ahead 80 | 0 |
| `mono-ask` | ahead 126 | 0 |
| `mono-oss` | ahead 19 | 1 |
| `main` | ahead 1 | 1 |
| `mono-base-25.12`, `mono-ask-25.12`, `mono-oss-25.12`, `selinux-policy-r9` | — | 0 |

Going by the ahead counts would have implied ~110 commits at risk; the real
number was 1 commit plus one unpushed branch. **The `main`/`mono-oss` straggler
is the same commit on both** — `dc8b76cad9`, authored by `github-actions[bot]`,
"main: track upstream source without imported workflows". That is the bot's own
restore commit, regenerated by `prepare` on every run, so the remote already
carries a newer one. Discardable.

**`feature/maya-w2-wifi-build-support` had never been pushed** — no upstream at
all. 13 commits absent by patch-id, but 12 are April-era ancestors whose
content has since been rewritten on `mono-ask` (inferred from subjects and
dates; not diffed individually). One is genuinely unique and exists nowhere
else: `035ac671b5 openwrt: add MAYA-W2 Wi-Fi build support` — 9 files, 517
insertions, three new packages (`nxp-maya-w2-firmware`, `nxp-mxm-wifi`,
`maya-w2-wifi-diag`) plus `docs/04-maya-w2-wifi-build-support.md`.

Pushed on user instruction, then verified: patch-missing 0 against the new
remote, trees identical, `035ac671b5` confirmed present on
`origin/feature/maya-w2-wifi-build-support`. No local branch in `openwrt/`
lacks an upstream any more. The branch stays dormant (requester unreachable) —
it is simply no longer destructible.

## Agent-instruction changes

- **`CLAUDE.md` → `AGENTS.md` symlink** created in the workspace, plus
  `!/CLAUDE.md` in `.gitignore` (the allowlist would otherwise have swallowed
  it exactly as it swallowed `HANDOFF.md`). Motivation: `AGENTS.md` was **not
  being loaded into Claude Code sessions at all** — confirmed by its absence
  from this session's initial context. **Not yet verified** that a fresh
  session picks the symlink up.
- Migrated durable rules from `HANDOFF.md` and the agent memory files into
  `AGENTS.md` (157 → 240 lines): new **Router Runtime Safety** and **Build
  Discipline** sections, plus extensions to **Evidence and Scope Discipline**
  and **Git Discipline**. Facts and current state were deliberately left out —
  only invariants moved.
- Possible overlap to review: the new root **Build Discipline** section versus
  the existing `openwrt/AGENTS.md` **Build Commands** section, which was not
  read before writing.

## Decisions

- **Retire the meta workspace.** Its original purpose was convenient access to
  the `ASK/` and `OpenWRT-ASK/` reference trees; its surviving purpose is
  multi-repo code intelligence over the four *owned* repos, which the reference
  trees are not even part of. Code intelligence needs `--root <directory>`, not
  a tracked meta-repo, so a plain sibling layout satisfies it.
- **`lab-snapshots/` and `openwrt-backup/` do not matter** and will not be
  migrated (user, this session). Not deleted — the wipe does that for free.
- **Journals become append-only, one file per session date.** `HANDOFF.md` was
  serving two consumers with opposite needs: a fresh session wanting ~20 lines
  of current state, and archaeology wanting a complete record. The
  consolidate-never-drop rule and its `PostToolUse` hook existed only because a
  single rewritable file had to do both. Splitting them retires that discipline
  entirely.

## This session's migration

The 15 session digests in `HANDOFF.md` were split verbatim into
`journal/2026-07-{18,19,20,25,26}.md` using `sed` line ranges rather than
retyping. Verified by token sweep: 28 distinctive commit hashes, register
names, and magic values (`0x886F`, `0x67c9df10`, `PHY_MIISTAT`, `Rollball`,
`rate 107000`, …) all still resolve; line accounting 345 → 349, the delta being
exactly the 4 blank separators inserted between concatenated sessions.

## Open / next

- `journal/` was written while `openwrt` was checked out on
  **`selinux-policy-r9`** — a feature branch. Needs relocating to the intended
  branch before commit.
- Branch-skew question unresolved: `docs/05-*` and `docs/06-*` exist only on
  `mono-ask`. Decide whether `journal/`, `STATE.md`, and migrated docs live on
  one branch or both.
- Still unmigrated and still remote-less: `AGENTS.md`, `HANDOFF.md`, `docs/`
  (including untracked `project-map.md` and `code-intelligence-pipeline.md`),
  `scripts/`, `tools/project-code-intelligence/`, the agent memory files, and
  `.claude/settings.local.json`.
